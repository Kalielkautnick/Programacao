MERGE INTO ESTOQUE_MATERIAL EST_MAT
	USING (
	   SELECT
	   PRO.ID_PRODUTO,
	   PRO.QUANTIDADE AS QTDE
	   FROM ESTOQUE_PRO PRO) EST_PRO
ON EST_MAT.ID_MATERIAL = EST_PRO.ID_PRODUTO
WHEN MATCHED THEN 
UPDATE SET EST_MAT.QUANTIDADE = (EST_PRO.QTDE - EST_MAT.QUANTIDADE)